{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "CreateAttatchmentRequest",
  "description": "Creates a attatchment upload url.",
  "type": "object",
  "properties": {
    "itemId": {
      "description": "Creates a upload url to attach a file to an item. If itemId is provided, then locationId is unnessary.",
      "type": "string",
      "format": "uuid"
    },
    "locationId": {
      "description": "Creates a upload url to attach a file to an location. If locationId is provided, then itemId is unnessary.",
      "type": "string",
      "format": "uuid"
    }
  },
  "anyOf": [
    {
      "required": [
        "itemIds"
      ]
    },
    {
      "required": [
        "locationId"
      ]
    }
  ],
  "not": {
    "required": [
      "itemIds",
      "locationId"
    ]
  },
  "additionalProperties": false
}